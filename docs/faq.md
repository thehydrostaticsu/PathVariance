# FAQ

**Does it run the agent?**
No. It reads repeated-run exports the harness already produces.

**Why refuse a verdict on few runs?**
Three runs cannot describe a distribution. The gate says so instead of
printing a misleading percentage.
