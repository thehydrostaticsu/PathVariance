# Argument sensitivity

With argument sensitivity off, runs that call the same tools in the same order
share a path. With it on, differing arguments make different paths. Use it off
to ask "same route"; use it on to ask "same decisions". The report records
which setting produced it, so two runs can never be confused.
